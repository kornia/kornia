Morphology dilation and erosion no longer use the finite `max_val` sentinel for floating-point reductions. Geodesic padding and masked-out kernel positions now use the appropriate infinite reduction identities, preventing finite sentinel values from leaking into results or limiting the representable output range.
