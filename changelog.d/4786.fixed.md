The conventions pages are trimmed to kornia scope: the augmentation, randomness and serialization sections are condensed with every tracked defect link kept, `RandomEqualize` and `RandomClahe` are documented as raising out of range on every device, the ARCore camera frame is merged into the OpenGL row whose `camtoworld_graphics_to_vision_*` converters apply to it, and the Direct3D row and the upstream-quote appendix are removed.
