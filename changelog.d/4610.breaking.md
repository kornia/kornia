`RandomMotionBlur` and `MotionBlurGenerator` now raise `ValueError` at construction for a reversed `kernel_size` range such as `(20, 3)`. Previously a reversed range was silently sampled as if it were ordered: `(20, 3)` drew the odd sizes `3` to `19`.
