`CenterCrop3D` and `RandomCrop3D` now accept a crop with a size-1 axis. The crop matrix is built as the translation it is, instead of being solved from the box vertices, which raised `_LinAlgError` because those vertices are coplanar.
