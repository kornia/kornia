Fixed non-finite gradients in `rotation_matrix_to_quaternion` and `rotation_matrix_to_axis_angle` near singular points (identity and 180° rotations) across `float16`, `bfloat16`, and `float32` by applying safe square-root gradient substitution matching #4613. Fixed `normalize_quaternion` gradient NaN leakage for exact zero-norm inputs and `eps=0.0` (note: subnormal-but-nonzero norms in `float16` remain out-of-scope pre-existing behavior).
