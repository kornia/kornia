Morphology dilation and erosion no longer leak the finite `max_val` sentinel into results for empty or masked windows. Floating-point reductions now use the appropriate infinite identity, while integer inputs use dtype extrema, so morphology results are no longer capped by the finite `max_val` value and remain consistent across the supported engines.
