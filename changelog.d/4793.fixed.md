Corrected the 2D intensity augmentation docstrings and the `kornia.enhance` `AdjustHue`, `AdjustContrastWithMeanSubtraction`, `AdjustSaturationWithGraySubtraction` and `AdjustBrightnessAccumulative` summaries: `RandomBrightness`, `RandomContrast` and `RandomSaturation` no longer claim to match PIL and torchvision (their formulas differ, and `ColorJitter` is the class whose steps use torchvision's), `RandomHue`, `RandomGamma` and the `enhance` modules state the mapping they do share with torchvision, the stale MPS and raw-error notes on `RandomEqualize` and `RandomClahe` are gone, and the convention blocks are trimmed to kornia's own behaviour. Newly found defects are linked as known issues: #4781, #4782, #4783, #4784, #4785, #4807, #4810, #4811 and #4813.
