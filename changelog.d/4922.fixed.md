`find_essential` returns ten `NaN` slots for a five-point sample without a real solution (no real root, or a singular or non-finite elimination), like the complex slots of any other sample, so `isfinite` filtering and `RANSAC` drop it. Before, it returned ten identity matrices, which are finite but not essential matrices. (#4883)
