The 3D augmentations now reject an angle range wider than one turn at construction, as the 2D ones do: `RandomRotation3D`, `RandomAffine3D` (`degrees` and `shears`) and `RandomMotionBlur3D` raise `ValueError` for a scalar or explicit range past `±360`, where they used to sample from it.
