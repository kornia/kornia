`RandomAffine` and `RandomPerspective` now compile on CUDA with CPU-generated geometry parameters, including numeric and CPU tensor constructor ranges, with or without moving the augmentation. Parameter placement, returned dtype, and RNG consumption are preserved. Dtype-only `.to()` calls on augmentations and random generators now preserve the sampler device; device-only calls preserve its dtype.
