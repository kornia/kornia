`RandomMixUpV2` and `RandomCutMixV2` now replay a `forward_parameters()` dictionary with the `"class"` key; the image dtype that the label handlers need is taken from the input when the dictionary has no `"dtype"`, instead of raising `KeyError: 'dtype'`.
