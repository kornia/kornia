`RandomMixUpV2` and `RandomCutMixV2` now replay a `forward_parameters()` dictionary with the `"class"` key; the image dtype that the label handlers need is taken from the input, instead of raising `KeyError: 'dtype'`. A replayed dictionary with an unsupported image dtype still raises `TypeError`.
