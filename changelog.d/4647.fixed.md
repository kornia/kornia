Speed up median blur inference for 3×3 and 5×5 kernels using selection networks on CPU, and on CUDA for 3×3 kernels or under `torch.compile`. Speed up CPU float32/float64 Laplacian filtering using separable neighborhood sums (with oneDNN, below 2^22 input elements), also on CUDA under `torch.compile`. Preserve median gradient tie behavior with the original autograd path. Replace dense and separable box convolutions with average pooling for floating-point inputs outside autocast, except where oneDNN's CPU convolution is faster: eager inputs below 2^22 elements, larger windows, and compiled windows wider than 5 (separable) or 3 (dense). Construct fixed first-order Sobel kernels directly without transposing, stacking, or normalizing them. Benchmark timing now honors the configured PyTorch thread count, and the filters flagship warms the CPU thread pool with sustained load so hybrid CPUs are measured on their performance cores. Use the large-image Gaussian slice shortcut in eager mode on CPUs with oneDNN, under `torch.compile` on CPUs without oneDNN, and under `torch.compile` on CUDA. Avoid padded-image copies for odd constant-border box windows, and redundant identity passes for one-dimensional box windows. Reuse Gaussian CPU accumulation buffers during eager execution (also accelerating unsharp masking), keep bilateral patch axes as views to avoid materialized patch copies, fuse guided-filter multiply/add operations, and vectorize 2D motion-kernel construction. Ordinary eager CPU box/Laplacian filtering uses a range check and convolution fallback for extreme values, preserving the optimized path for practical image ranges; captured graphs and function transforms keep their native arithmetic.
