`adjust_brightness`, `adjust_contrast`, `adjust_saturation`, `adjust_hue` and their variants now raise `ValueError` for a `factor` with more dimensions than the image, instead of hanging forever (#4718).
