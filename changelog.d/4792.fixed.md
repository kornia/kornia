`kornia.geometry.conversions` docstrings no longer claim that the deprecated aliases break `-W error::DeprecationWarning` (fixed in #3956), state the float64 axis-angle round-trip accuracy correctly (roundoff, about `2e-10` on the low-angle branch), and document two defects: `rotation_matrix_to_axis_angle` returns the identity for a reflection (#4773), and `Rt_to_matrix4x4` and `camtoworld_to_worldtocam_Rt` handle an `R`/`t` batch-size mismatch differently (#4774).
