`equalize_clahe(..., slow_and_differentiable=True)` no longer raises `IndexError` for `grid_size=(1, 1)`, which is global histogram equalization with the clip limit applied. Grids with more than one tile are unchanged.
