`crop_by_indices` now honors `shape_compensation` when every box in the batch is identical: a mismatched slice is padded rather than silently resized, so a row's output no longer depends on the other rows in the batch.
