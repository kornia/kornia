`CenterCrop`, `RandomCrop`, `center_crop` and `CenterCrop2D` now accept a crop with a size-1 axis. The crop matrix is built as the translation it is, instead of being solved from the box vertices, which returned a matrix of `NaN` because those vertices are collinear.
