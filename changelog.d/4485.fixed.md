`RandomCrop` with `padding` or `pad_if_needed` no longer shifts keypoints and bounding boxes by the
left/top padding when the crop is skipped; they previously moved while the image and mask stayed
unchanged. (#4473)
