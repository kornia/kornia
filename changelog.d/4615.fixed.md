Replaced `view` with `reshape` when flattening and restoring leading dimensions in perform_keep_shape_image and perform_keep_shape_video.
Added regression tests for non-contiguous image and video inputs.