`solve_quadratic`, `solve_cubic` and `solve_quartic` now solve a polynomial with a zero leading coefficient as the lower-degree polynomial it is. Before, `solve_quadratic` returned `nan`/`inf` for a linear equation, `solve_cubic` returned `1.0` for every linear equation and zeros for `bx² + d`, and `solve_quartic` inherited both. (#4873)
