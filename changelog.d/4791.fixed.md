The `kornia.augmentation` Convention blocks outside the 2D intensity classes are shorter and state each rule once. `RandomEqualize3D` now documents its out-of-range check as raising on every backend; the old text said MPS skipped it. Known defects are listed once each with their tracking issue, including the new #4775, #4776, #4777 and #4787.
