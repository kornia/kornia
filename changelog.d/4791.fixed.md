The `kornia.augmentation` Convention blocks outside the 2D intensity classes are shorter and state each rule once. `RandomEqualize3D` now documents its out-of-range check as raising on every backend; the old text said MPS skipped it. Each known defect is stated with its tracking issue, including the new #4775, #4776, #4777, #4787, #4801, #4802, #4804, #4805, #4809, #4814 and #4815.
