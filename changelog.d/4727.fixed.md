`CenterCrop` in the default `cropping_mode="slice"` now returns a copy of the cropped region instead of a view of the
input, so writing to the output no longer modifies the input tensor. Fixes #4413.
