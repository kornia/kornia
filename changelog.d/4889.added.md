Convention blocks for the projection and structure functions of `kornia.geometry.epipolar` (`projection_from_KRt`, `KRt_from_projection`, `projections_from_fundamental`, `depth_from_point`, `scale_intrinsics`, `intrinsics_like`, `random_intrinsics`, `triangulate_points`, `generate_scene`), three functions added to the rendered API reference, and 16 convention and wart pins; the #4263 wart is documented and linked, not fixed; #4863, #4864, #4865 and #4900 are fixed by #4901.
