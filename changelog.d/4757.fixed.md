`RandomCutMixV2` now draws each cut start uniformly over all legal positions: the last two positions per axis were unreachable before, and a full-image cut (`cut_size=(0.0, 0.0)`) no longer places the box one pixel outside the image.
