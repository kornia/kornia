Document the conventions of `RandomTransplantation` and `RandomTransplantation3D` — mask-driven donor selection, the `(i - 1) mod B` donor rule, label sampling and `excluded_labels`, the replay contract, and the data-key and dtype rules — with executable tests, and export both names from `kornia.augmentation.__all__`.
