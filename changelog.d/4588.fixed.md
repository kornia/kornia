RandomCrop now uses the effective pre-crop canvas for oversized requests. With `RandomCrop((10, 10), padding=1)` on a 3-by-3 input, the scale changes from `10/3` to `10/5`; the no-padding behavior tracked in #4414 is unchanged.
