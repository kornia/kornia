`Quaternion.slerp` takes the shorter arc between the two rotations whatever the signs of the stored quaternions (it used to go the long way round when their dot product was negative), and its gradient is finite when both endpoints are equal. (#4944)
