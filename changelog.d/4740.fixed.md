`RandomTransplantation` and `RandomTransplantation3D` accept a mask with a singleton channel axis next to an image of the same rank, so inside `AugmentationSequential` they no longer raise when another augmentation runs before them.
