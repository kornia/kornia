`RandomTransplantation` and `RandomTransplantation3D` accept a `(B, 1, *spatial)` mask next to an image of the same rank and read it as the `(B, *spatial)` mask, so `RandomTransplantation` no longer raises inside `AugmentationSequential` when another augmentation runs before it.
