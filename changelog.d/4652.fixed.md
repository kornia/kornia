`RandomMosaic` now respects `output_size` when transforming bounding boxes, preserves the
correct number of boxes for partially selected batches without adding phantom boxes, and no
longer crashes with `cropping_mode="resample"` when `output_size=None`. Fixes #4652.
