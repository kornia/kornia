Improve the performance of the morphology `shift` engine on the forward-only path by updating the running maximum/minimum in place. This avoids allocating a new output-sized tensor for each kernel position while preserving the existing out-of-place reduction path for backward autograd and forward-mode AD. The optimized path produces identical results to the autograd-safe path. (#4745)
