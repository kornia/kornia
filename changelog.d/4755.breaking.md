`adjust_gamma` and `AdjustGamma` now raise `ValueError` when `gamma` or `gain` has more dimensions than the image, matching the other factor ops. Previously the over-ranked factor silently broadcast the image up to a larger output shape.
