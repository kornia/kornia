`geometry.transform` docstrings: removed the false `crop_by_boxes` note claiming a source box smaller than the destination box raises, and documented two known defects with their issues: `homography_warp(normalized_homography=False)` ignores `mode` and `align_corners` (#4772), and `remap` resamples under an identity pixel map at its default `align_corners` (#4504).
