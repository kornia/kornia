`HausdorffERLoss` and `HausdorffERLoss3D` launch fewer kernels per erosion step: the negative part is clamped with `clamp_min` instead of a boolean index assignment, and an all-ones mask is no longer built and multiplied in. Loss values and gradients are unchanged.
