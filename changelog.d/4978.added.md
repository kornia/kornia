Convention blocks for `relative_transformation` and its linalg siblings (frame naming, rigid-only inputs, the contrast with `relative_camera_motion`) and for `NamedPose`, an API page for `kornia.geometry.pose`, and 11 convention and wart pins; the warts are documented and linked (#4881, #4937), not fixed.
