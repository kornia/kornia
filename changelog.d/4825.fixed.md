`kornia.augmentation.__all__` now lists `RandomClahe` and `RandomJPEG`, so `from kornia.augmentation import *` includes them. Both classes were importable by name before but had never been in `__all__`.
