`ColorJiggle(order=...)` no longer turns off `torch.distributions` argument validation for the rest of the process. Its eager `torch.cond` dispatch entered Dynamo, whose one-time setup disables that validation globally; the caller's setting is now restored.
