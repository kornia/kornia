`kornia.morphology` gains `engine="shift"`: a running max or min over the shifted views of the padded image. Its output is bitwise identical to `engine="unfold"`, it needs no kernel-area-sized intermediate tensor, and `torch.compile` fuses it into a single kernel.
