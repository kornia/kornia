Corrected the 2D intensity augmentation docstrings: `RandomBrightness`, `RandomContrast` and `RandomSaturation` no longer claim to match PIL and torchvision (their formulas differ, and `ColorJitter` is the class whose steps use torchvision's), `RandomHue` and `RandomGamma` state the mapping they do share with torchvision, the stale MPS and raw-error notes on `RandomEqualize` and `RandomClahe` are gone, and the convention blocks are trimmed to kornia's own behaviour, with newly found defects linked as known issues.
