`KMeans.fit` no longer loops over clusters with `torch.nonzero`/`index_select` to update centroids; it now computes per-cluster sums and counts with a one-hot assignment matrix and a single matmul. This removes a host sync on every cluster of every iteration and is faster on both CPU and MPS.
