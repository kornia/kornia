`ColorJitter` skips a step whose factors are all neutral again, as it did before v0.9.0rc1, instead of computing every step and discarding the neutral ones: `ColorJitter(brightness=0.2)` accepts one- and four-channel images again, and an eager call no longer pays for the neutral colour-space round trips. Outputs for inputs that were accepted before are unchanged (#4813).
