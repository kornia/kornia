Convention blocks for `So2`, `Se2` and `Se3` (tangent order, direction, `log` range, adjoint and matrix layouts, composition order) and 21 convention and wart pins; the warts are documented and linked (#4923, #4929, #4931, #4932, #4942), not fixed.
