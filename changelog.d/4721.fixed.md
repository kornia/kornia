`RandomCutMixV2` now draws the cut placement per row and mix when `same_on_batch=False`; one uniform draw per axis used to place every cut of the batch, so all cuts sat at the same relative position. The random stream for a fixed seed changes when `B * num_mix > 1`.
