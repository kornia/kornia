`solve_cubic` now returns the real root of a cubic whose depressed form is `t^3 = 2R` with `R < 0` (for example `x^3 + 1`) instead of NaN. `solve_quartic` inherited the NaN through its cubic fallback. (#4832)
