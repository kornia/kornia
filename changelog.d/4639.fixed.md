Setup documentation (`CONTRIBUTING.md`, `README.md`, `README_zh-CN.md`, `AGENTS.md`, `docs/export_support/README.md`) now qualifies the ambiguous Pixi tasks with `-e default`, so `pixi run install`, `pixi run install-docs` and `pixi run build-docs` work as documented instead of stopping with a "task is ambiguous" error or an environment picker.
