The contributing guide now qualifies the ambiguous Pixi setup tasks with `-e default`, so `pixi run install`, `pixi run install-docs` and `pixi run build-docs` work as documented instead of stopping with a "task is ambiguous" error or an environment picker.
