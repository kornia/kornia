`normalize_transformation`, `find_homography_dlt`, and `find_homography_lines_dlt` now normalize by the actual last entry without epsilon bias, leaving estimates with near-zero last entries unscaled and keeping the normalization finite for autograd.
